#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_hello_world() {
        // Example unit test
        let result = "Hello World from Rust";
        assert_eq!(result, "Hello World from Rust");
    }

    // More tests will go here as we build features
}
