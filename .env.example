# Vite built-in variables (Vite sets DEV/PROD/SSR automatically, but can be included for clarity)
# BASE_URL=/
# DEV=true
# PROD=false
# SSR=false

# Custom environment variables (must start with VITE_)
REACT_APP_NODE_ENV=test

# Example placeholders for future usage
# VITE_API_URL=https://api.example.com
# VITE_API_KEY=your-api-key-here
